export const runtime = "edge"; // Use edge runtime for better performance
export const maxDuration = 60; // Increase execution time if needed
export const bodyParser = { sizeLimit: "50mb" }; // Increase upload limit
